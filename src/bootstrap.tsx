import React from 'react';

export default function Bootstrap() {
    return (
        <div style={{ padding: 50, background: '#eee', minHeight: '100vh', color: '#333' }}>
            <h1>🚀 EMERGENCY BOOTSTRAP</h1>
            <p>If you see this, the server is running correctly.</p>
            <p>The problem is in the main 'App' component logic.</p>
        </div>
    )
}
